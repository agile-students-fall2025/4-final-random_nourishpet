# MongoDB Connection (REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nourishpet?retryWrites=true&w=majority

# JWT Secret (REQUIRED)
# Generate a random string for signing JWT tokens
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# Server Port (OPTIONAL - defaults to 3001)
PORT=3001

# CORS Allowed Origins (REQUIRED for production)
# Comma-separated list of allowed origins
# For local development:
# ALLOWED_ORIGINS=http://localhost:3000,http://frontend:80
# For production droplet (UPDATE WITH YOUR ACTUAL IP):
ALLOWED_ORIGINS=http://167.172.223.98:3000,http://localhost:3000,http://frontend:80

# Groq AI API (OPTIONAL - required for meal plan generation and BMI calculations)
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.1-8b-instant

# Email Service (OPTIONAL - required for password reset functionality)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
# Update FRONTEND_URL to match your droplet URL for production
FRONTEND_URL=http://167.172.223.98:3000
